# Dependencias
node_modules/
.npm/

# Logs
*.log
npm-debug.log*

# Entornos
.env
.env.local
.env.*.local

# Editores / SO
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code — worktrees, sesiones y memoria local nunca viajan al repo.
# EXCEPCIÓN: .claude/settings.json SÍ se commitea — son los hooks del cerebro
# (brain-check --boot + session-handoff) y deben viajar con el repo.
.claude/*
!.claude/settings.json
.claude/settings.local.json

# Build / caches
dist/
build/
.cache/
.vite/

# 🧠 Cerebro — sidecars efímeros escritos por los hooks de sesión
docs/.handoff-auto.md
docs/.estado-auto.md
docs/.boot-marker
# ⚠️ scripts/.kernel-version.json SÍ se commitea: es el sello de integridad del kernel (gate #0).

# Secretos / claves — NUNCA al repo
*.key
*.pem
serviceAccount*.json
service-account*.json
*-service-account*.json
gcp-*.json
firebase-adminsdk*.json
.runtimeconfig.json

# Firebase locales
.firebase/
*-debug.log

# ⚠️ CONFIDENCIAL — datos reales de cliente (AFINIA / EPM).
# Nunca al repo: van a almacenamiento privado o a la bóveda ../brain-private.
# El HTML original del módulo de campo pesa 30 MB y lleva 99 fotos de campo
# embebidas en base64: es material de cliente, no código.
/_entrada/
/datos-cliente/
*_Modulo_Campo_*.html
LN-*.html
*.gpx
*.kml
# Fotografías de campo sueltas
/fotos/
*.CR2
*.NEF

# Build del frontend
web/dist/

# Sello de compilación incremental de TypeScript: se regenera solo y no es del proyecto.
*.tsbuildinfo

# ⚠️ Registro fotográfico de campo — DATO DE CLIENTE. Nunca al repositorio público.
# Lo puso aquí el Ingeniero el 17-08; vive en la bóveda privada (ver docs/10).
LN-627/
**/Registro Fotografico/
fotos-del-portero/

# Estado local de wrangler dev: nunca al repositorio (es PÚBLICO)
.wrangler/

# ⚠️ EXPORTACIONES DE SCADA — DATO DE CLIENTE. Nunca al repositorio público.
# El 07-09 el Ingeniero dejó aquí «Variables Electricas/Enero/01» con siete
# archivos de la RED ENTERA (corrientes, potencias y tensiones de decenas de
# subestaciones). No llegó a subirse: lo paró el guardián de coordenadas, que
# no está para esto y acertó por los pelos. Ahora se para antes.
# Viven en `brain-private/mantenimiento-lineas-at/Variables Electricas/`.
Variables Electricas/
**/Variables Electricas/
*_max-2[0-9][0-9][0-9][0-9][0-9][0-9][0-9].csv
